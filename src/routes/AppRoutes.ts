import { RoutesConfiguration } from '../core/types/router.types';
import { applicantRoutesConfig } from '../routes/Applicant/ApplicantRoutes';

export const appRoutes: RoutesConfiguration = [...applicantRoutesConfig];
